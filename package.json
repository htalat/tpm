{
  "name": "tpm",
  "version": "0.11.0",
  "description": "Markdown-based task & project manager. CLI-driven, agent-friendly.",
  "type": "module",
  "private": true,
  "bin": {
    "tpm": "./bin/tpm"
  },
  "scripts": {
    "test": "node --test 'src/**/*.test.ts'",
    "loop": "node scripts/loop.ts",
    "release": "./scripts/release.sh"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
