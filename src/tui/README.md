# tui

Reserved for the terminal UI — the in-terminal equivalent of `tpm serve`'s web
dashboard. Empty today; the folder exists so the eventual TUI has an obvious home
alongside `web/` (web dashboard), `core/` (orchestration + task tree), and
`util/` (dependency-free helpers).
